{
  "name": "krb5",
  "version": "0.1.0",
  "description": "Kerberos library for Node.js, with Spnego Wrapping",
  "main": "index.js",
  "keywords": [
    "kerberos",
    "security",
    "gssapi",
    "spnego",
    "authentication"
  ],
  "dependencies": {
    "coffee-script": "latest"
  },
  "scripts": {
    "install": "(coffee -c index.coffee && node-gyp configure && node-gyp rebuild 2> builderror.log) || (exit 0)"
  },
  "author": {
    "name": "Pierre Sauvage"
  }
}
